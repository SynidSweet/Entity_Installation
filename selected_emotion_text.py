def onTableChange(dat):
    # Get the selected emotion
    selected_emotion = dat.text
    # Perform actions based on the selected emotion
    # (For example, you can use this to drive visual or audio elements)
    print(f"Selected Emotion: {selected_emotion}")
    return

